import { css } from 'lit';

// language=CSS
export const cliCommandsStyles = css`
  cc-block-details a {
    color: var(--cc-color-text-primary-highlight, blue);
  }
  cc-block-details p {
    margin: 0;
  }
`;
