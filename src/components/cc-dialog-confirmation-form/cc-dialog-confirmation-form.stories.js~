import { makeStory, storyWait } from '../../stories/lib/make-story.js';
import './cc-dialog-confirmation-form.js';

export default {
  tags: ['autodocs'],
  title: '=à Dialog/<cc-dialog-confirmation-form>',
  component: 'cc-dialog-confirmation-form',
};

/**
 * @typedef {import('./cc-dialog-confirmation-form.js').CcDialogConfirmationForm} CcDialogConfirmationForm
 */

const conf = {
  component: 'cc-dialog-confirmation-form',
};

export const defaultStory = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Confirm',
    },
  ],
});

export const simpleConfirmation = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Confirm',
      submitIntent: 'primary',
    },
  ],
});

export const simpleConfirmationWithDanger = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Delete',
      submitIntent: 'danger',
    },
  ],
});

export const simpleConfirmationWithCustomCancelLabel = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Continue',
      submitIntent: 'primary',
      cancelLabel: 'Go back',
    },
  ],
});

export const confirmationWithInput = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Delete',
      submitIntent: 'danger',
      confirmInputLabel: 'Type the name to confirm',
      confirmTextToInput: 'my-app',
    },
  ],
});

export const confirmationWithInputAndAutofocus = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Delete',
      submitIntent: 'danger',
      confirmInputLabel: 'Type the name to confirm',
      confirmTextToInput: 'my-app',
      autofocusInput: true,
    },
  ],
});

export const confirmationWithInputAndLongText = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Delete permanently',
      submitIntent: 'danger',
      confirmInputLabel: 'Type the full name to confirm deletion',
      confirmTextToInput: 'my-very-long-application-name-that-is-difficult-to-type',
    },
  ],
});

export const waiting = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Confirm',
      submitIntent: 'primary',
      waiting: true,
    },
  ],
});

export const waitingWithInput = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Delete',
      submitIntent: 'danger',
      confirmInputLabel: 'Type the name to confirm',
      confirmTextToInput: 'my-app',
      waiting: true,
    },
  ],
  /** @param {CcDialogConfirmationForm} component */
  onUpdateComplete: (component) => {
    // TODO: not working
  const input = component.shadowRoot.querySelector('cc-input-text');
    input.value = 'my-app';
  },
});

export const simulationsWithSubmit = makeStory(conf, {
  /** @type {Partial<CcDialogConfirmationForm>[]} */
  items: [
    {
      submitLabel: 'Confirm',
      submitIntent: 'primary',
    },
  ],
  simulations: [
    storyWait(
      2000,
      /** @param {CcDialogConfirmationForm[]} components */
      ([component]) => {
        component.waiting = true;
      },
    ),
    storyWait(
      4000,
      /** @param {CcDialogConfirmationForm[]} components */
      ([component]) => {
        component.waiting = false;
      },
    ),
  ],
});
