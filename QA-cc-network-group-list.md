| Name | Action details | Expectation | Chrome | FF | Safari | Remarks | Category |
|------|----------------|-------------|--------|-----|--------|---------|----------|
| Skeleton mode (form + list) | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section | Both form and list display skeleton loading states | | | | | Loading states |
| Error loading | 1. Block requests to `api.clever-cloud.com`<br>2. Go to https://console-staging-three.clever-cloud.com<br>3. Navigate to application/add-on settings → Network Groups section | Both form and list display error notices | | | | | Error loading page |
| Form reset on success | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Select a network group from dropdown<br>4. Click "Link" button | 1. Form resets (dropdown refreshed with remaining options)<br>2. Linked network groups list is refreshed<br>3. Selected network group appears in the list | | | | | Pattern Verification |
| Error preserves form data | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Select a network group from dropdown<br>4. Block requests to `api.clever-cloud.com`<br>5. Click "Link" button | 1. Error toast is displayed<br>2. Form returns to idle state<br>3. Selected value is preserved in dropdown | | | | | Pattern Verification |
| State cleanup verification | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Trigger link action<br>4. Disconnect network mid-request | Loading state should reset on all paths (success refreshes, error returns to idle) | | | | | Pattern Verification |
| Success notification | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Select a network group<br>4. Click "Link" button | A success toast is displayed after linking | | | | | Pattern Verification |
| Error notification | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Select a network group<br>4. Block API requests<br>5. Click "Link" button | An error toast is displayed | | | | | Pattern Verification |
| Link network group (success) | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Select a network group from dropdown<br>4. Click "Link" button | 1. Button shows waiting state<br>2. Network group is linked to resource<br>3. Success toast is displayed<br>4. List is refreshed with new linked group<br>5. Dropdown options are updated (linked group removed) | | | | | Functional Test |
| Link network group (error) | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Select a network group from dropdown<br>4. Block requests to `api.clever-cloud.com`<br>5. Click "Link" button | 1. Button shows waiting state<br>2. Error toast is displayed<br>3. Form returns to idle state<br>4. Selected value is preserved | | | | | Functional Test |
| Empty list state | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to a resource with no linked network groups | List section shows empty state message | | | | Storybook: dataLoadedWithEmpty | Functional Test |
| No available network groups to link | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to a resource where all network groups are already linked | Form section shows "Create" link instead of dropdown | | | | Storybook: dataLoadedWithNoNetworkGroupToLink | Functional Test |
| Network group card displays correctly | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Observe linked network group cards | Each card displays:<br>- Network group name<br>- Network group ID with copy button<br>- Dashboard link<br>- List of peers | | | | | Functional Test |
| Network group peer cards | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Expand a linked network group with peers | Peer cards display peer information correctly | | | | | Functional Test |
| Dashboard link navigation | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Click dashboard link on a network group card | Navigates to network group dashboard page | | | | | Functional Test |
| Copy network group ID | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Click copy button on network group ID | Network group ID is copied to clipboard | | | | | Functional Test |
| Select options sorted alphabetically | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Open the network group dropdown | Options are sorted alphabetically by name | | | | | Functional Test |
| Double-click submit prevention | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Select a network group<br>4. Rapidly double-click "Link" button | Only one request is sent, no duplicate links | | | | cc-button blocks clicks when waiting=true | Edge Cases |
| Navigate away during link request | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Select a network group<br>4. Click "Link" button<br>5. Immediately navigate away<br>6. Return to component | No console errors, component in clean state | | | | | Edge Cases |
| Network timeout handling | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Throttle network to very slow<br>4. Trigger link action | Request eventually completes or times out gracefully with error toast | | | | | Edge Cases |
| Figma design compliance | 1. Go to https://console-staging-three.clever-cloud.com<br>2. Navigate to application/add-on settings → Network Groups section<br>3. Compare with Figma design | Component matches Figma design: https://www.figma.com/design/F5yUZ8WL7P6Eb6IFdCXzo7/Network-Groups | | | | | Design Verification |
